const { getStringInput } = require('../../utils')

const input = getStringInput()

const left = {
    rock: 'A',
    paper: 'B',
    scissor: 'Z',
}

const right = {
    rock: 'X',
    paper: 'Y',
    scissor: 'Z',
}

console.log(input)
